[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma migrate deploy && npx prisma db seed && next start -H 0.0.0.0 -p ${PORT:-3000}"
healthcheckPath = "/login"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
